apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: ksail
volumes:
  - volume: k3d-ksail-storage:/var/lib/rancher/k3s/storage
network: k3d-ksail
options:
  k3s:
    extraArgs:
      - arg: "--disable=traefik"
        nodeFilters:
          - server:*
